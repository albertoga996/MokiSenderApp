<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mokisenderapp">

    <!--permission android:name="com.example.broadcast.moki.mypermission"
        android:label="my_permission"
        android:protectionLevel="dangerous" />

    <uses-permission android:name="com.example.broadcast.moki.mypermission" /-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".derived.SenderApp"
        tools:ignore="LockedOrientationActivity"
        android:theme="@style/Theme.MokiSenderApp">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>